<line
  stroke="#00BE92"
  stroke-width="0.1rem"
  stroke-dasharray="<%= "#{0.2 * Graph.base} #{0.4 * Graph.base}" %>"
  x1="0"
  y1="<%= tredline.b %>"
  x2="<%= Graph.chart_width %>"
  y2="<%= tredline.a * Graph.chart_width + tredline.b %>"
/>
