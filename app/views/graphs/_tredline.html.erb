<line
  stroke="#1FDCC5"
  stroke-width="0.1rem"
  stroke-dasharray="<%= "#{0.5 * Graph.base} #{0.5 * Graph.base}" %>"
  x1="0"
  y1="<%= tredline.b %>"
  x2="<%= Graph.chart_width %>"
  y2="<%= tredline.a * Graph.chart_width + tredline.b %>"
/>
