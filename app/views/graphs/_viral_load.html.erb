<polygon
  fill="#020202"
  stroke="none"
  stroke-width="4"
  points="
    0, <%= Graph.chart_height %>
    <% points.each_with_index do |point| %>
      <%= "#{point.x}, #{point.y - 10}," %>
    <% end %>
    <%= "#{points.last.x}, #{Graph.chart_height}," %>
    0, <%= Graph.chart_height %>
  "
/> %>
