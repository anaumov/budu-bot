<polygon
  fill="#020202"
  stroke="none"
  stroke-width="4"
  points="
    0, 300
    <% points.each_with_index do |point| %>
      <%= "#{point.x}, #{point.y - 10}," %>
    <% end %>
    <%= "#{points.last.x}, 300," %>
    0, 300
  "
/> %>
